<%= simple_form_for(@redcarpet, html: {class: "form-horizontal"}) do |f| %>

 <%= f.input :title %>
 <%= f.input :published_at, as: :string %>
  <div class="form-actions">
    <%= f.button :submit, class: "btn" %>
  </div>
<% end %>
